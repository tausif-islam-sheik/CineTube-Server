enum Role {
    USER
    ADMIN
}

enum PricingType {
    FREE
    PREMIUM
}

enum ReviewStatus {
    PENDING
    APPROVED
    REJECTED
}

enum UserStatus {
    ACTIVE
    DELETED
    BLOCKED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}

enum PaymentMethod {
    STRIPE
    SSLCOMMERZ
    WALLET
}

enum SubscriptionStatus {
    ACTIVE
    CANCELLED
    EXPIRED
    PAUSED
}

enum SubscriptionTierName {
    FREE
    STANDARD
    PREMIUM
    VIP
}

enum AccessType {
    RENTAL
    STREAMING
}
